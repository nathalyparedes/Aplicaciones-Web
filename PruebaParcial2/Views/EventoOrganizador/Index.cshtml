@model IEnumerable<PruebaParcial2.Models.EventoOrganizador>

@{
    ViewData["Title"] = "Lista de Eventos y Organizadores";
}

<h1>Eventos y Organizadores</h1>

<p class="text-left">
    <a asp-action="Create" class="btn btn-primary">Nuevo Registro</a>
</p>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Evento.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Organizador.Nombre)
            </th>
            <th class="text-center">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Evento.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Organizador.Nombre)
                </td>
                <td class="text-center">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Editar</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">Detalles</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>