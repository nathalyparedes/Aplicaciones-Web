@model PruebaParcial2.Models.EventoParticipante

@{
    ViewData["Title"] = "Eliminar Participante de Evento";
}

<h1 class="text-center text-danger">Eliminar Participante de Evento</h1>

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-body">
                <h4 class="text-muted text-center">¿Estás seguro de que deseas eliminar este registro?</h4>
                <p class="text-center text-danger">Esta acción no se puede deshacer.</p>
                <hr />
                <dl class="row">
                    <dt class="col-sm-4">Evento</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Evento.Nombre)</dd>

                    <dt class="col-sm-4">Fecha de Inscripción</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.FechaInscripcion)</dd>

                    <dt class="col-sm-4">Participante</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Participante.Nombre)</dd>
                </dl>
            </div>
            <div class="card-footer d-flex justify-content-between">
                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger">Eliminar</button>
                </form>
                <a asp-action="Index" class="btn btn-secondary">Volver a la lista</a>
            </div>
        </div>
    </div>
</div>