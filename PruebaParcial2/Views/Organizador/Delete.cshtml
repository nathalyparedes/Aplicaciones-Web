@model PruebaParcial2.Models.Organizador

@{
    ViewData["Title"] = "Eliminar Organizador";
}

<h1 class="text-center text-danger">Eliminar Organizador</h1>

<h3 class="text-center">¿Estás seguro de que deseas eliminar este organizador?</h3>
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm">
            <div class="card-body">
                <h4 class="text-muted">Detalles del Organizador</h4>
                <hr />
                <dl class="row">
                    <dt class="col-sm-4">Nombre</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Nombre)</dd>
                    <dt class="col-sm-4">Tipo de Organización</dt>
                    <dd class="col-sm-8">@Html.DisplayFor(model => model.TipoOrganizacion)</dd>
                </dl>
                <form asp-action="Delete" class="mt-3">
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex justify-content-between">
                        <a asp-action="Index" class="btn btn-secondary">Volver a la lista</a>
                        <input type="submit" value="Eliminar" class="btn btn-danger" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>