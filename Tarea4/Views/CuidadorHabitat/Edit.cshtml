@model Tarea4.Models.CuidadorHabitat

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CuidadorHabitat</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="CuidadorId" class="control-label"></label>
                <select asp-for="CuidadorId" class="form-control" asp-items="ViewBag.CuidadorId"></select>
                <span asp-validation-for="CuidadorId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HabitatId" class="control-label"></label>
                <select asp-for="HabitatId" class="form-control" asp-items="ViewBag.HabitatId"></select>
                <span asp-validation-for="HabitatId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
